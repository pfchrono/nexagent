# Phase 14.1 Context

Goal: give `nexagent` a unique spinner/emblem and progress chrome instead of stopping at donor verb import.

Repo truth before this phase:
- spinner verbs were donor-imported from free-code
- progress line still used generic single-character spinner prefix
- only first few imported verbs were actually reachable because animation index wrapped too early
- unique emblem direction was captured in planning, not code

Source refs used:
- `.planning/notes/spinner-emblem-direction.md`
- `.planning/todos/pending/spinner-emblem-hybrid-spec.md`
- `/home/pfchrono/code/free-code/src/constants/spinnerVerbs.ts`

Scope for `14.1-01`:
- add unique `nexagent` emblem frames
- make progress chrome use emblem + donor verb list
- ensure full spinner verb list is actually reachable over time
- keep progress line driven by shared runtime action state

Truth boundary:
- this is progress chrome polish, not full custom theme system
- donor verb vocabulary remains, donor visual chrome does not
