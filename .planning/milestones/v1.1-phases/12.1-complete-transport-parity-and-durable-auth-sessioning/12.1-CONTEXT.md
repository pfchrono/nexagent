# Phase 12.1 Context

Goal: finish transport parity honestly by separating OpenAI-key HTTP responses transport from Codex CLI transport, while adding durable local session state and first real Codex OAuth refresh handling.

Repo truth before this slice:
- Phase 12 only exposed CLI-exec transport truth
- no durable session state for provider/transport preference
- no command to switch transport mode explicitly
- no HTTP responses adapter at all
- Codex OAuth backend and OpenAI key backend were still being conflated

Source refs used:
- `/home/pfchrono/code/codex-fresh/codex-rs/codex-client/src/chatgpt_cloudflare_cookies.rs`
- `/home/pfchrono/code/codex-fresh/codex-rs/login/src/auth/agent_identity.rs`
- `/home/pfchrono/code/free-code/src/services/api/providerConfig.ts`
- `/home/pfchrono/code/free-code/src/services/api/codex-fetch-adapter.ts`

Important donor truth:
- OpenAI key path uses `https://api.openai.com/v1/responses`
- ChatGPT/Codex backend path uses `https://chatgpt.com/backend-api/codex/responses`
- those are not same transport and should not share fake adapter labels

Scope for `12.1-01`:
- add persisted runtime session state
- add explicit transport mode switching
- add OpenAI-key HTTP responses adapter on separate seam
- add first Codex backend HTTP transport using local `~/.codex/auth.json` token/account state
- refresh expired Codex OAuth access token from stored refresh token and rewrite local auth store
- add donor-backed local model catalog so API transport support is checked against real Codex model truth
- keep current Codex CLI exec path as default

Out of scope for `12.1-01`:
- remote/refreshed model catalog import from `codex-fresh`
