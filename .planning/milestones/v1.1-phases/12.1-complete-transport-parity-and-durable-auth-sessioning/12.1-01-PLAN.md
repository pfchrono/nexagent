---
phase: "12.1-complete-transport-parity-and-durable-auth-sessioning"
plan: "12.1-01"
type: "feature"
wave: 1
depends_on:
  - "12-01"
files_modified:
  - "src/runtime/persistence.ts"
  - "src/provider/codex-http.ts"
  - "src/provider/codex-chatgpt-http.ts"
  - "src/runtime/bootstrap.ts"
  - "src/runtime/session.ts"
  - "src/provider.ts"
  - "src/cli.ts"
  - "src/models.ts"
  - "test/provider.test.ts"
  - "test/cli.test.ts"
  - "test/runtime-config.test.ts"
autonomous: true
must_haves:
  truths:
    - "OpenAI-key HTTP responses transport is separate from Codex CLI exec transport."
    - "Codex backend HTTP transport is separate from OpenAI-key HTTP transport."
    - "API transport model support is checked against donor model truth instead of guessed slugs."
    - "Runtime can persist provider and transport preference without breaking command flow on read-only paths."
    - "Operator can inspect and switch transport mode explicitly."
    - "Codex backend HTTP transport refreshes expired local OAuth access tokens through `~/.codex/auth.json` instead of pretending sessions never expire."
    - "Remote model catalog refresh remains unfinished rather than hidden."
---

<objective>
Start Phase 12.1 with transport seam and durable local state, including first direct Codex backend HTTP path, without pretending refresh/session lifecycle is complete.
</objective>

<success_criteria>
- runtime persists provider + transport preference in `.nexagent/session.json`
- `/provider transport ...` can inspect and switch mode
- `http-responses` uses OpenAI key semantics and `/v1/responses`
- `codex-http` uses local Codex auth semantics and `chatgpt.com/backend-api/codex/responses`
- API transports reject donor-marked unsupported models like `gpt-5.3-codex-spark`
- `codex-http` refreshes expired local OAuth access token using stored refresh token and rewrites `~/.codex/auth.json`
- tests and build pass
</success_criteria>
