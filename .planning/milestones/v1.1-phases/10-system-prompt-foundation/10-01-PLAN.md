---
phase: "10-system-prompt-foundation"
plan: "10-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/runtime/instructions.ts"
  - "src/cli.ts"
  - "test/instructions.test.ts"
  - "test/provider.test.ts"
  - "test/runtime-config.test.ts"
  - "test/cli.test.ts"
autonomous: true
must_haves:
  truths:
    - "Runtime prompt has explicit system identity and execution guidance."
    - "Prompt assembly exposes stable versus dynamic sections through one shared structure."
    - "Dynamic user-turn content is separated by explicit boundary marker."
    - "Existing inspect and TUI surfaces keep working with richer summaries."
  artifacts:
    - path: "src/runtime/instructions.ts"
      provides: "Section-based prompt assembly with dynamic boundary and serialized stable/dynamic split."
      min_lines: 80
      contains: "SYSTEM_PROMPT_DYNAMIC_BOUNDARY"
    - path: "src/cli.ts"
      provides: "Inspect and TUI visibility for expanded instruction summaries."
      min_lines: 40
      contains: "stableSections"
    - path: "test/instructions.test.ts"
      provides: "Regression coverage for section order, defaults, and serialized boundary."
      min_lines: 40
      contains: "dynamicBoundary"
    - path: "test/provider.test.ts"
      provides: "Provider prompt payload assertions for new system-prompt shape."
      min_lines: 20
      contains: "Explicit invocation"
  key_links:
    - from: "src/runtime/instructions.ts"
      to: "src/cli.ts"
      via: "Instruction summaries and section metadata flow into inspect/TUI rows."
      pattern: "stableSections|dynamicSections|identity|executionGuidance"
    - from: "test/provider.test.ts"
      to: "src/runtime/instructions.ts"
      via: "Provider payload test locks boundary marker and explicit invocation serialization."
      pattern: "SYSTEM_PROMPT_DYNAMIC_BOUNDARY|Explicit invocation"
---

<objective>
Land first real system-prompt foundation for `v1.1`.
</objective>

<tasks>
<task id="1" type="auto">
  <name>
  Add modular prompt sections
  </name>
  <description>
  Extend runtime prompt assembly from loose layer buckets into explicit system-prompt sections with stable and dynamic cache modes.
  </description>
  <files>
  src/runtime/instructions.ts
  test/instructions.test.ts
  test/runtime-config.test.ts
  </files>
  <verify>
  npm test -- test/instructions.test.ts
  npm test -- test/runtime-config.test.ts
  </verify>
</task>

<task id="2" type="auto">
  <name>
  Surface section summaries
  </name>
  <description>
  Keep inspect and TUI aligned with richer prompt truth.
  </description>
  <files>
  src/cli.ts
  test/cli.test.ts
  </files>
  <verify>
  npm test -- test/cli.test.ts
  </verify>
</task>

<task id="3" type="auto">
  <name>
  Lock provider payload shape
  </name>
  <description>
  Ensure provider request assembly reflects new prompt layout without changing provider routing.
  </description>
  <files>
  test/provider.test.ts
  src/runtime/instructions.ts
  </files>
  <verify>
  npm test -- test/provider.test.ts
  </verify>
</task>

<success_criteria>
- Runtime prompt now contains system identity and execution guidance.
- Dynamic invocation is separated by `__SYSTEM_PROMPT_DYNAMIC_BOUNDARY__`.
- Inspect and TUI show section summaries and stable/dynamic section lists.
- Provider prompt tests and build stay green.
</success_criteria>
