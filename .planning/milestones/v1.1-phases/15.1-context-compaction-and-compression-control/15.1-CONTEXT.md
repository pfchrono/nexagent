# Phase 15.1 Context

Goal: add first real context retention and compaction control to `nexagent` instead of keeping turns only in visible TUI transcript.

Repo truth before this phase:
- provider requests only carried current prompt plus system prompt layers
- prior turns were visible in TUI transcript only, not fed back into runtime prompt assembly
- no manual `/compact`
- no auto compaction threshold
- no remaining-token visibility

Source refs used:
- `.planning/notes/context-compaction-design.md`
- `.planning/todos/pending/context-compaction-runtime-spec.md`
- `src/runtime/session.ts`
- `src/runtime/instructions.ts`
- `src/models.ts`
- `src/cli.ts`

Scope for `15.1-01`:
- add conversation state to runtime session
- add manual `/compact`
- add auto compaction threshold from model context window percentage
- include compact summary + recent turns in prompt assembly
- show remaining-token and compaction status in runtime views

Truth boundary:
- compaction summary is deterministic local summary, not model-written summary yet
- queued-message handling exists around synchronous compaction before provider request
- normal-turn steering still unsupported in current runtime, but compact-turn restriction is explicit
