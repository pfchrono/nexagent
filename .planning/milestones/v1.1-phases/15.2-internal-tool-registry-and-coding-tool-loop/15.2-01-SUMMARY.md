# Phase 15.2 Summary

Status: complete

What landed:
- added shared internal tool registry in `src/runtime/tools.ts`
- added tool definitions and prompt guidance for:
  - `read_file`
  - `list_dir`
  - `search_content`
  - `search_files`
  - `git_status`
- internal tools enforce existing repo-local readonly policy and protected roots
- `search_content` and `search_files` use `rg` when present with JS fallback paths
- provider loop in `src/provider.ts` now handles bounded `<nexagent_tool_call>...</nexagent_tool_call>` exchanges
- slash wrappers in `src/cli.ts` now call shared internal handlers instead of duplicating local logic
- runtime status/tool policy output now shows internal tool inventory

Truth boundary:
- tool loop uses harness-managed XML protocol, not provider-native structured function calling
- first slice is readonly inspection only
- no shell/write/edit/delete tools yet

Verification:
- `bun test test/provider.test.ts`
- `bun test test/cli.test.ts`
- `bun test test/instructions.test.ts`
- `npm run build`

Outcome:
- model now has real repo-local internal read/search/list/git inspection tools
- slash commands remain optional operator surface over same shared runtime path
