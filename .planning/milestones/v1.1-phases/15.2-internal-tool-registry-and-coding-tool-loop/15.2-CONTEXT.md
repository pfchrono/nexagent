# Phase 15.2 Context

Goal: replace slash-command-only local tooling as primary mechanism with true internal tools callable during coding turns.

Why now:
- phase 15 established repo-local readonly tool policy and protected roots
- phase 15.1 established conversation carry-forward and compaction state
- runtime still lacked model-callable repo inspection tools

Donor direction:
- `free-code` for local tool inventory and tool-first harness behavior
- `hermes-agent` for registry/dispatcher shape and `rg`-first search behavior

Scope kept small:
- internal tool registry with schemas
- first coding tools: `read_file`, `list_dir`, `search_content`, `search_files`, `git_status`
- provider-side tool loop using strict XML tool-call envelope
- slash wrappers reuse shared handlers instead of duplicating logic

Out of scope:
- provider-native function calling
- write/edit/delete tools
- shell command tool
- web/MCP pass-through tool loop
