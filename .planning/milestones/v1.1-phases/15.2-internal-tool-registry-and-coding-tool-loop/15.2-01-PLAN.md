# Plan 15.2-01

1. Add shared internal tool module with schema metadata and repo-policy-aware handlers.
2. Inject explicit tool protocol guidance into assembled system prompt.
3. Add provider-managed internal tool loop with bounded retries.
4. Repoint slash wrappers at shared handlers so repo tools have one implementation path.
5. Add regression tests for prompt guidance, tool loop, and wrapper reuse.
6. Verify with focused tests and build.
