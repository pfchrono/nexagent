---
phase: "12-codex-adapter-and-provider-transport-upgrade"
plan: "12-01"
type: "feature"
wave: 1
depends_on:
  - "11-01"
files_modified:
  - "src/provider/codex-exec.ts"
  - "src/provider.ts"
  - "src/runtime/bootstrap.ts"
  - "src/runtime/session.ts"
  - "src/cli.ts"
  - "test/provider.test.ts"
  - "test/cli.test.ts"
  - "test/runtime-config.test.ts"
autonomous: true
must_haves:
  truths:
    - "Codex transport lives behind dedicated adapter module, not inline provider code."
    - "Shared runtime state exposes adapter, mode, auth source, and auth gate."
    - "Provider transport truth stays consistent across inspect, GUI, TUI, and command output."
    - "Failure output names real transport and adapter instead of vague transport-only state."
---

<objective>
Land explicit Codex adapter transport metadata and shared visibility across runtime surfaces.
</objective>

<success_criteria>
- `src/provider/codex-exec.ts` owns Codex CLI exec transport logic.
- Provider results include adapter metadata on success and failure.
- Inspect, GUI, TUI, and `/provider` surface adapter/mode/auth-gate truth.
- Targeted tests and build pass.
</success_criteria>
