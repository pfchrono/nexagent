# v1.3 Summary

Status: complete

Major outcomes:
- live Codex HTTP turn path restored and made default when auth JSON is available
- steer state/history became explicit and boundary-aware
- transport capability/caveat truth became visible in runtime surfaces
- bounded `git_diff` tool added to internal coding surface
- Archivist retrieval ranking improved to prefer explicit useful memory over generic checkpoint noise
- TTY moved from dashboard-first toward workspace-first with visible cursor, prompt history, and compact event transcript rendering

Verification:
- `bun test test/provider.test.ts`
- `bun test test/cli.test.ts`
- `npm run build`

Result:
- `nexagent` now behaves more like operator-facing daily harness than raw runtime baseline

Truth boundary:
- `codex-cli-exec` transport still bounded but unreliable in this environment
- TTY still lacks scrollback pane, mouse expand/collapse, and dedicated approval card UI
- future multimodal clipboard-image path not implemented
