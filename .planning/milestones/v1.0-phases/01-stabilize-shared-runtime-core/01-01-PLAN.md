---
phase: "01-stabilize-shared-runtime-core"
plan: "01-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/runtime/session.ts"
  - "src/cli.ts"
  - "test/runtime-config.test.ts"
  - "test/cli.test.ts"
autonomous: true
must_haves:
  truths:
    - "Shared runtime owns provider selection truth during reload and command handling."
    - "Shared runtime owns action/progress updates through one helper path."
    - "CLI remains thin over shared session helpers."
  artifacts:
    - path: "src/runtime/session.ts"
      provides: "Provider-selection and action helpers shared across interfaces."
      min_lines: 30
      contains: "applyProviderSelection"
    - path: "src/cli.ts"
      provides: "CLI paths routed through shared session helpers instead of duplicating runtime mutations."
      min_lines: 80
      contains: "setRuntimeAction"
    - path: "test/runtime-config.test.ts"
      provides: "Regression coverage for reload/provider transport preservation and shared action helper behavior."
      min_lines: 40
      contains: "syncRuntimeSession preserves selected provider in transport state after reload"
    - path: "test/cli.test.ts"
      provides: "Regression coverage for command-mode action updates."
      min_lines: 40
      contains: "runPromptCommand updates shared action state for runtime commands"
---

<objective>
Harden shared runtime session truth so reload and command flows do not fork provider or action state.
</objective>

<context>
Runtime bootstrap and session state already exist. Remaining phase-1 gap is consistency: provider selection and action/progress state still had duplicate mutation paths in CLI, and reload could desync transport-facing active provider from selected provider.
</context>

<tasks>
<task id="1" type="auto">
  <name>
  Centralize provider selection in session layer
  </name>
  <description>
  Ensure selected provider survives reload and stays aligned across routing and transport state.
  </description>
  <files>
  src/runtime/session.ts
  src/cli.ts
  test/runtime-config.test.ts
  </files>
  <action>
  Add shared provider-selection helper in runtime session layer, use it in runtime sync and provider command handling, and lock reload behavior with a regression test.
  </action>
  <verify>
  npm test -- test/runtime-config.test.ts
  </verify>
  <done>
  Selected provider remains aligned in provider, providerRouting, and providerTransport after reload.
  </done>
</task>

<task id="2" type="auto">
  <name>
  Centralize runtime action state updates
  </name>
  <description>
  Move action/progress mutation into shared session helper so command mode and TUI flows use same runtime truth.
  </description>
  <files>
  src/runtime/session.ts
  src/cli.ts
  test/runtime-config.test.ts
  test/cli.test.ts
  </files>
  <action>
  Add shared action helper, route CLI command and provider request paths through it, and add tests for command-mode action updates and helper semantics.
  </action>
  <verify>
  npm test -- test/cli.test.ts
  npm test -- test/runtime-config.test.ts
  </verify>
  <done>
  Runtime commands and provider requests update one shared action state shape.
  </done>
</task>
</tasks>

<verification>
- `npm test -- test/runtime-config.test.ts`
- `npm test -- test/cli.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Shared runtime owns provider truth during reload and command handling.
- Shared runtime owns action/progress updates through one helper path.
- CLI stays a thin consumer of shared session state.
</success_criteria>
