---
phase: "06-expand-tool-execution-surface"
plan: "06-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/cli.ts"
  - "test/cli.test.ts"
autonomous: true
must_haves:
  truths:
    - "Local commands are available as harness behavior."
    - "Tool commands are usable from both prompt mode and TUI through shared command path."
    - "Filesystem inspection and repo search are treated as core harness behavior."
  artifacts:
    - path: "src/cli.ts"
      provides: "Repo-aware local command handlers for pwd, ls, read, and find."
      min_lines: 120
      contains: "handleFindCommand"
    - path: "test/cli.test.ts"
      provides: "Regression coverage for local tool commands."
      min_lines: 40
      contains: "runRuntimeCommand supports local tool commands"
---

<objective>
Add first real local tool commands so harness can inspect repo state without immediately delegating to provider execution.
</objective>

<context>
Current command surface mostly exposed provider state. This slice adds deterministic local tool commands through existing shared slash-command path, making CLI and TUI act more like harness interfaces.
</context>

<tasks>
<task id="1" type="auto">
  <name>
  Add filesystem and search commands
  </name>
  <description>
  Expose local cwd, directory listing, file reading, and text search through slash commands.
  </description>
  <files>
  src/cli.ts
  test/cli.test.ts
  </files>
  <action>
  Extend `runRuntimeCommand` with `/pwd`, `/ls`, `/read`, and `/find`, resolve paths from session cwd, and add regression tests that exercise those commands against temporary repo content.
  </action>
  <verify>
  npm test -- test/cli.test.ts
  </verify>
  <done>
  Local tool commands work through shared command path and are covered by tests.
  </done>
</task>
</tasks>

<verification>
- `npm test -- test/cli.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Local commands, filesystem actions, and repo search are available as harness behavior.
- Tool contracts are usable across existing interfaces through shared slash-command handling.
- Search/tool ergonomics are treated as core behavior in current baseline.
</success_criteria>
