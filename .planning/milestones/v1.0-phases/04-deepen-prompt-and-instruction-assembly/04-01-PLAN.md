---
phase: "04-deepen-prompt-and-instruction-assembly"
plan: "04-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/runtime/bootstrap.ts"
  - "src/runtime/config.ts"
  - "src/cli.ts"
  - "test/instructions.test.ts"
  - "test/cli.test.ts"
autonomous: true
must_haves:
  truths:
    - "Operator can see which instruction sources materially influenced a request."
    - "Prompt assembly stays layered until provider serialization."
    - "Inspect output and TUI show same instruction-assembly truth."
    - "Provider routing remains unchanged in this slice."
  artifacts:
    - path: "src/runtime/config.ts"
      provides: "Instruction-source summaries with meaningful content for repo files and structured summaries for directory-backed sources."
      min_lines: 40
      contains: "instruction"
    - path: "src/runtime/bootstrap.ts"
      provides: "Shared assembled instruction layers preserved through bootstrap before provider serialization."
      min_lines: 40
      contains: "provider"
    - path: "src/cli.ts"
      provides: "Inspect and TUI rendering consume shared instruction-assembly data."
      min_lines: 80
      contains: "inspect"
    - path: "test/instructions.test.ts"
      provides: "Regression coverage for source summaries, precedence, and serialization boundaries."
      min_lines: 40
      contains: "instruction"
    - path: "test/cli.test.ts"
      provides: "Regression coverage for inspect output and rendered TUI instruction views."
      min_lines: 40
      contains: "inspect"
  key_links:
    - from: "src/cli.ts"
      to: "src/runtime/bootstrap.ts"
      via: "Inspect output and TUI read shared assembled instruction data from runtime bootstrap."
      pattern: "bootstrap|assembled|instruction"
    - from: "src/runtime/bootstrap.ts"
      to: "src/runtime/config.ts"
      via: "Bootstrap consumes structured instruction-source summaries and layered config state."
      pattern: "instruction|source|layer"
    - from: "test/cli.test.ts"
      to: "src/cli.ts"
      via: "CLI tests assert inspect and TUI instruction views."
      pattern: "inspect|instruction|tui"
    - from: "test/instructions.test.ts"
      to: "src/runtime/bootstrap.ts"
      via: "Instruction tests lock precedence and provider-serialization boundaries."
      pattern: "precedence|provider|instruction"
---

<objective>
Make prompt assembly inspectable without widening provider routing.
</objective>

<context>
Current 04-01 draft already scopes this slice to richer instruction summaries, shared inspect/TUI visibility, and explicit layering boundaries before provider serialization. Keep transport semantics, provider selection, hooks, memory, and command-surface expansion out of scope.
</context>

<tasks>
<task id="1" type="auto">
  <name>
  Upgrade instruction-source summaries
  </name>
  <description>
  Upgrade instruction-source summaries so assembled prompt output names material inputs instead of only reporting file or directory presence.
  </description>
  <files>
  src/runtime/config.ts
  src/runtime/bootstrap.ts
  test/instructions.test.ts
  </files>
  <action>
  Extend instruction-source summary generation in src/runtime/config.ts to emit meaningful file-backed and directory-backed summaries. Keep outputs structured enough for reuse by inspect and TUI consumers. Add or update tests in test/instructions.test.ts to lock these summary shapes.
  </action>
  <verify>
  npm test -- test/instructions.test.ts
  </verify>
  <done>
  Repo-file summaries and directory-backed summaries are asserted in test/instructions.test.ts.
  </done>
</task>

<task id="2" type="auto">
  <name>
  Share inspect and TUI instruction state
  </name>
  <description>
  Surface assembled instruction layers through inspect output and current TUI views using one shared runtime truth.
  </description>
  <files>
  src/cli.ts
  src/runtime/bootstrap.ts
  src/runtime/config.ts
  test/cli.test.ts
  </files>
  <action>
  Update src/runtime/bootstrap.ts and src/cli.ts so inspect output and TUI rendering both consume the same assembled instruction-layer structure instead of deriving separate views. Add or update test/cli.test.ts assertions for both output paths.
  </action>
  <verify>
  npm test -- test/cli.test.ts
  </verify>
  <done>
  Inspect output and TUI instruction rendering are both asserted in test/cli.test.ts.
  </done>
</task>

<task id="3" type="auto">
  <name>
  Preserve layered instruction precedence
  </name>
  <description>
  Preserve explicit boundaries between invocation overrides, repo behavior, task context, imported defaults, tool availability, and provider fallback until provider serialization.
  </description>
  <files>
  src/runtime/bootstrap.ts
  src/runtime/config.ts
  test/instructions.test.ts
  </files>
  <action>
  Refine layered prompt assembly in src/runtime/bootstrap.ts and any supporting config logic so precedence remains explicit until the final provider payload is built. Add regression coverage in test/instructions.test.ts for precedence ordering and serialization boundaries.
  </action>
  <verify>
  npm test -- test/instructions.test.ts
  </verify>
  <done>
  Precedence ordering and provider-serialization boundaries are locked by test/instructions.test.ts.
  </done>
</task>

<task id="4" type="auto">
  <name>
  Keep provider routing unchanged
  </name>
  <description>
  Keep provider routing unchanged while landing inspectability work.
  </description>
  <files>
  src/cli.ts
  src/runtime/bootstrap.ts
  test/cli.test.ts
  test/instructions.test.ts
  </files>
  <action>
  Limit code changes to prompt assembly, summary generation, and instruction presentation. Do not add new transport behavior or broaden provider-selection logic in this slice. Use tests to prove routing behavior did not change while new inspection surfaces landed.
  </action>
  <verify>
  npm test -- test/instructions.test.ts
  npm test -- test/cli.test.ts
  </verify>
  <done>
  No new provider-selection branches land beyond instruction-visibility plumbing, and instruction-related tests pass together.
  </done>
</task>
</tasks>

<verification>
- `npm test -- test/instructions.test.ts`
- `npm test -- test/cli.test.ts`
- `node /home/pfchrono/.claude/get-shit-done/bin/gsd-tools.cjs verify artifacts .planning/phases/04-deepen-prompt-and-instruction-assembly/04-01-PLAN.md`
- `node /home/pfchrono/.claude/get-shit-done/bin/gsd-tools.cjs verify key-links .planning/phases/04-deepen-prompt-and-instruction-assembly/04-01-PLAN.md`
</verification>

<success_criteria>
- Operator can see which instruction sources materially influenced a request.
- Prompt assembly remains layered before provider serialization.
- Inspect output and TUI both surface the same instruction-assembly truth.
- Tests lock precedence and serialization behavior.
- Provider routing behavior is unchanged in this slice.
</success_criteria>
