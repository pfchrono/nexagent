---
phase: "07-add-hooks-plugins-and-repo-local-automation"
plan: "07-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/runtime/config.ts"
  - "src/runtime/bootstrap.ts"
  - "src/cli.ts"
  - "test/runtime-config.test.ts"
  - "test/cli.test.ts"
autonomous: true
must_haves:
  truths:
    - "Repo-local hook policy is explicit and inspectable."
    - "Hook config problems are visible instead of hidden."
    - "Hook visibility flows through shared runtime state, not interface-specific logic."
  artifacts:
    - path: "src/runtime/config.ts"
      provides: "Hook config parsing and imported hook metadata."
      min_lines: 120
      contains: "mapClaudeHooks"
    - path: "src/runtime/bootstrap.ts"
      provides: "Shared runtime hook state."
      min_lines: 40
      contains: "hooks"
    - path: "src/cli.ts"
      provides: "Hook inspection in command surface, TUI, GUI, and inspect payload."
      min_lines: 120
      contains: "handleHooksCommand"
    - path: "test/runtime-config.test.ts"
      provides: "Regression coverage for imported hook config."
      min_lines: 40
      contains: "importedKeys: [\"provider\", \"modelSelection\", \"enabledMcpServers\", \"hooks\"]"
    - path: "test/cli.test.ts"
      provides: "Regression coverage for hook visibility through runtime commands and GUI/TUI views."
      min_lines: 40
      contains: "\"hooks status\""
---

<objective>
Expose repo-local automation hooks as explicit shared runtime policy.
</objective>

<context>
Repo already has Claude-style hooks, but runtime did not model them explicitly. This slice adds hook-policy parsing and visibility while avoiding a premature execution engine.
</context>

<tasks>
<task id="1" type="auto">
  <name>
  Parse imported hook policy
  </name>
  <description>
  Import Claude hook configuration into shared runtime config with invalid-entry tracking.
  </description>
  <files>
  src/runtime/config.ts
  src/runtime/bootstrap.ts
  test/runtime-config.test.ts
  </files>
  <action>
  Add hook config parsing to Claude import path, expose normalized hook state in runtime bootstrap, and add regression tests for imported hook metadata.
  </action>
  <verify>
  npm test -- test/runtime-config.test.ts
  </verify>
  <done>
  Shared runtime exposes hook events, command count, and invalid-entry visibility.
  </done>
</task>

<task id="2" type="auto">
  <name>
  Surface hook policy across interfaces
  </name>
  <description>
  Make hook state inspectable from command mode, TUI, GUI, and inspect payload.
  </description>
  <files>
  src/cli.ts
  test/cli.test.ts
  </files>
  <action>
  Add `/hooks` command and hook sections/rows to existing runtime views, with tests proving hook visibility stays aligned across surfaces.
  </action>
  <verify>
  npm test -- test/cli.test.ts
  </verify>
  <done>
  Hook policy is inspectable without introducing interface-specific state.
  </done>
</task>
</tasks>

<verification>
- `npm test -- test/runtime-config.test.ts`
- `npm test -- test/cli.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Hook execution policy is explicit and inspectable.
- Hook config problems are surfaced instead of hidden.
- Repo-local policy boundaries are preserved through shared runtime visibility.
</success_criteria>
