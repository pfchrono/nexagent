---
phase: "08-add-memory-and-persisted-harness-state"
plan: "08-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/runtime/config.ts"
  - "src/cli.ts"
  - "test/runtime-config.test.ts"
  - "test/cli.test.ts"
  - "test/provider.test.ts"
autonomous: true
must_haves:
  truths:
    - "Persistence boundaries are explicit and transparent."
    - "Repo-local Archivist config still overrides imported defaults."
    - "Interfaces can inspect persisted-memory boundary and storage-file presence."
  artifacts:
    - path: "src/runtime/config.ts"
      provides: "Archivist boundary and persisted-file presence in shared config."
      min_lines: 120
      contains: "boundary"
    - path: "src/cli.ts"
      provides: "Memory inspection command and archivist visibility rows."
      min_lines: 120
      contains: "handleMemoryCommand"
    - path: "test/runtime-config.test.ts"
      provides: "Regression coverage for Archivist boundary fields."
      min_lines: 40
      contains: "boundary: \"read-only\""
    - path: "test/cli.test.ts"
      provides: "Regression coverage for `/memory` and archivist visibility."
      min_lines: 40
      contains: "\"memory status\""
---

<objective>
Expose persisted-memory boundaries and storage-file presence without claiming durable write/retrieval behavior that does not exist yet.
</objective>

<context>
Archivist config already exists, but runtime did not clearly show whether memory was disabled, read-only configured, or backed by a present storage file. This slice makes that boundary visible and explicit.
</context>

<tasks>
<task id="1" type="auto">
  <name>
  Materialize Archivist persistence boundary
  </name>
  <description>
  Add boundary mode and storage-file existence to shared Archivist state.
  </description>
  <files>
  src/runtime/config.ts
  test/runtime-config.test.ts
  test/provider.test.ts
  </files>
  <action>
  Extend Archivist config with `boundary` and `storageExists`, keep repo-local override behavior intact, and update tests to lock new shared state shape.
  </action>
  <verify>
  npm test -- test/runtime-config.test.ts
  </verify>
  <done>
  Shared runtime exposes explicit memory boundary and persisted-file presence.
  </done>
</task>

<task id="2" type="auto">
  <name>
  Surface memory status across interfaces
  </name>
  <description>
  Make memory boundary inspectable from command mode and runtime views.
  </description>
  <files>
  src/cli.ts
  test/cli.test.ts
  </files>
  <action>
  Add `/memory` command and extend archivist view rows with boundary and persisted-file presence in inspect/TUI/GUI.
  </action>
  <verify>
  npm test -- test/cli.test.ts
  </verify>
  <done>
  Operator can inspect current memory boundary and persisted-file presence through all runtime surfaces.
  </done>
</task>
</tasks>

<verification>
- `npm test -- test/runtime-config.test.ts`
- `npm test -- test/cli.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Persistence boundaries are explicit and transparent.
- Repo-local memory controls override globals.
- Interfaces can inspect persisted-memory boundary and storage-file presence.
</success_criteria>
