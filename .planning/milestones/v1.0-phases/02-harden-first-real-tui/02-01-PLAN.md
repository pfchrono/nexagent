---
phase: "02-harden-first-real-tui"
plan: "02-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/cli.ts"
  - "test/cli.test.ts"
autonomous: true
must_haves:
  truths:
    - "TUI progress uses shared runtime action state instead of local duplicate fields."
    - "Agent panel reflects shared runtime pending/detail state."
    - "Recent runtime activity is visible in TUI metadata."
  artifacts:
    - path: "src/cli.ts"
      provides: "TUI rendering driven by shared runtime action state and last-activity visibility."
      min_lines: 100
      contains: "lastActivity"
    - path: "test/cli.test.ts"
      provides: "Regression coverage for TUI metadata visibility."
      min_lines: 40
      contains: "lastActivity"
---

<objective>
Make current terminal UI a dependable readout of shared runtime action state.
</objective>

<context>
Current TUI already exposes major runtime sections, but progress and pending state still relied on local UI-only fields. Phase 2 should remove that split and render progress from shared runtime action truth.
</context>

<tasks>
<task id="1" type="auto">
  <name>
  Derive TUI progress from shared runtime action state
  </name>
  <description>
  Eliminate local progress duplication and use shared action state for status line and agent panel.
  </description>
  <files>
  src/cli.ts
  test/cli.test.ts
  </files>
  <action>
  Replace local TUI progress fields with shared action state, wire navigation/reload/request flows through that state, and add regression coverage for metadata visibility.
  </action>
  <verify>
  npm test -- test/cli.test.ts
  </verify>
  <done>
  TUI progress and agent-panel status derive from shared runtime action state.
  </done>
</task>
</tasks>

<verification>
- `npm test -- test/cli.test.ts`
- `npm run build`
</verification>

<success_criteria>
- TUI shows provider, repo/cwd, MCP, session, and instruction visibility from shared runtime.
- Progress uses shared action state rather than local duplicate fields.
- Runtime activity timing is visible in TUI metadata.
</success_criteria>
