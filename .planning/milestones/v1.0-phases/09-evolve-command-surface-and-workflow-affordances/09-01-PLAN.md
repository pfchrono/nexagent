---
phase: "09-evolve-command-surface-and-workflow-affordances"
plan: "09-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/cli.ts"
  - "test/cli.test.ts"
autonomous: true
must_haves:
  truths:
    - "Implemented command surface is explicit and inspectable."
    - "Unknown command paths guide operator toward supported runtime behavior."
    - "No broader command semantics are implied beyond implemented commands."
  artifacts:
    - path: "src/cli.ts"
      provides: "Central command catalog and help command."
      min_lines: 120
      contains: "COMMAND_CATALOG"
    - path: "test/cli.test.ts"
      provides: "Regression coverage for help output and unknown-command guidance."
      min_lines: 40
      contains: "runRuntimeCommand exposes command catalog through help"
---

<objective>
Make implemented command surface explicit and self-describing.
</objective>

<context>
Current runtime command behavior was real but not centrally exposed. This slice adds a small explicit command contract through `/help` and clearer unknown-command handling.
</context>

<tasks>
<task id="1" type="auto">
  <name>
  Add explicit command catalog
  </name>
  <description>
  Provide inspectable built-in command guidance without inventing broader command behavior.
  </description>
  <files>
  src/cli.ts
  test/cli.test.ts
  </files>
  <action>
  Add a central command catalog, expose it through `/help`, and route unknown-command errors toward that help path with regression tests.
  </action>
  <verify>
  npm test -- test/cli.test.ts
  </verify>
  <done>
  Operator can inspect supported command surface directly from runtime.
  </done>
</task>
</tasks>

<verification>
- `npm test -- test/cli.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Command semantics are explicit through implemented help surface.
- Command and automation effects remain routed through shared runtime state.
- Compatibility wording is not mistaken for broader implemented command behavior.
</success_criteria>
