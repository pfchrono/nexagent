---
phase: "03-harden-gui-shell"
plan: "03-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/cli.ts"
  - "test/cli.test.ts"
autonomous: true
must_haves:
  truths:
    - "GUI consumes same session and runtime truth as TUI."
    - "GUI parity is explicit in code and tests."
    - "No GUI-only state path is introduced."
  artifacts:
    - path: "src/cli.ts"
      provides: "Explicit GUI view factory that reuses TUI runtime view generation."
      min_lines: 100
      contains: "createRuntimeGuiView"
    - path: "test/cli.test.ts"
      provides: "Regression coverage for GUI/TUI view parity."
      min_lines: 40
      contains: "createRuntimeGuiView reuses the same runtime view contract as TUI"
---

<objective>
Make GUI shell explicitly downstream of same runtime view contract as TUI.
</objective>

<context>
GUI renderer already used same row-based sections as TUI, but there was no explicit factory proving both interfaces share one runtime-to-view path. This slice makes that contract concrete without adding GUI-only state.
</context>

<tasks>
<task id="1" type="auto">
  <name>
  Make GUI view parity explicit
  </name>
  <description>
  Add explicit GUI view factory that reuses TUI view generation and lock it with tests.
  </description>
  <files>
  src/cli.ts
  test/cli.test.ts
  </files>
  <action>
  Export `createRuntimeGuiView(session)` as a thin wrapper over `createRuntimeTuiView(session)` and add regression coverage that GUI/TUI view outputs stay identical for same session.
  </action>
  <verify>
  npm test -- test/cli.test.ts
  </verify>
  <done>
  GUI and TUI consume same runtime view contract through explicit API.
  </done>
</task>
</tasks>

<verification>
- `npm test -- test/cli.test.ts`
- `npm run build`
</verification>

<success_criteria>
- GUI consumes same session and runtime truth as TUI.
- GUI control-plane visibility remains grounded in shared runtime view data.
- No GUI-only state path exists.
</success_criteria>
