---
phase: "00-keep-baseline-honest"
plan: "00-01"
type: "docs"
wave: 1
depends_on: []
files_modified:
  - "AGENTS.md"
  - "CLAUDE.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
autonomous: true
must_haves:
  truths:
    - "Docs describe current repo truthfully."
    - "Missing OpenSpec change tree stays labeled missing instead of referenced as source of truth."
    - "Compatibility language stays constrained to files that exist."
  artifacts:
    - path: "AGENTS.md"
      provides: "Repo operating guidance that points at live planning and code files."
      min_lines: 20
      contains: "Plan.md"
    - path: "CLAUDE.md"
      provides: "Assistant compatibility guidance grounded in current repo shape."
      min_lines: 40
      contains: "Primary source of truth"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 0 marked complete with plan record."
      min_lines: 40
      contains: "Phase 0"
    - path: ".planning/STATE.md"
      provides: "Active phase advanced after phase 0 close-out."
      min_lines: 5
      contains: "Active phase"
---

<objective>
Remove fake baseline references so repo guidance matches files that actually exist.
</objective>

<context>
Live repo guidance still pointed agents at deleted OpenSpec change files under openspec/changes/define-nexagent-baseline. That broke phase 0 requirement that docs and compatibility language stay honest about current implementation state.
</context>

<tasks>
<task id="1" type="auto">
  <name>
  Rewrite AGENTS guidance around live files
  </name>
  <description>
  Point repo operating guidance at current repo-local planning and control-plane files instead of missing OpenSpec paths.
  </description>
  <files>
  AGENTS.md
  </files>
  <action>
  Replace deleted OpenSpec baseline references with current planning anchors like Plan.md, .planning/ROADMAP.md, .planning/STATE.md, and existing repo-local guidance files.
  </action>
  <verify>
  rg -n "define-nexagent-baseline" AGENTS.md
  </verify>
  <done>
  AGENTS.md contains no live guidance pointing at missing baseline files.
  </done>
</task>

<task id="2" type="auto">
  <name>
  Rewrite CLAUDE compatibility truth boundary
  </name>
  <description>
  Keep assistant compatibility guidance honest about repo shape and current design references.
  </description>
  <files>
  CLAUDE.md
  </files>
  <action>
  Replace missing OpenSpec change-tree references with current repo truth sources and preserve constrained wording around implemented runtime surfaces.
  </action>
  <verify>
  rg -n "define-nexagent-baseline|provider-routing/spec|command-automation/spec|instruction-assembly/spec|nexagent-harness/spec" CLAUDE.md
  </verify>
  <done>
  CLAUDE.md references only files that exist in current repo when naming source-of-truth documents.
  </done>
</task>

<task id="3" type="auto">
  <name>
  Advance planning state from real phase 0
  </name>
  <description>
  Record phase-0 completion so later autonomous work starts from honest repo state.
  </description>
  <files>
  .planning/ROADMAP.md
  .planning/STATE.md
  </files>
  <action>
  Add a phase-0 plan record, mark phase 0 complete, and move active state to phase 1.
  </action>
  <verify>
  rg -n "Phase 0|00-01|Active phase" .planning/ROADMAP.md .planning/STATE.md
  </verify>
  <done>
  Roadmap and state show phase 0 complete and phase 1 active.
  </done>
</task>
</tasks>

<verification>
- `rg -n "define-nexagent-baseline|provider-routing/spec|command-automation/spec|instruction-assembly/spec|nexagent-harness/spec" AGENTS.md CLAUDE.md`
- `rg -n "Phase 0|00-01|Active phase" .planning/ROADMAP.md .planning/STATE.md`
- `npm run build`
</verification>

<success_criteria>
- Live repo guidance points only at files that exist.
- Compatibility wording stays grounded in current repo shape.
- Phase 0 is recorded complete in planning artifacts.
</success_criteria>
