# Plan 25-01

1. Add operation-control state to runtime session and persistence.
2. Gate guarded internal tools behind optional approval mode.
3. Add `/approval`, `/cancel`, and `/steer` command surfaces.
4. Show operation state in `/status`, TUI, inspect, and provider loop activity.
5. Verify approval wait path, control commands, full tests, and build.
