# Plan 21-01

1. Export internal tool definitions as provider-native function schemas.
2. Add native Responses tool request payload support on `http-responses`.
3. Run native function-call loop with shared internal handlers.
4. Keep xml fallback for unsupported transports.
5. Add provider regression coverage and verify build.
