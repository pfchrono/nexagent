# Plan 23-01

1. Add `shell_command` to internal tool registry and provider function schemas.
2. Enforce repo-pinned cwd, destructive-command blocking, timeout, and output caps.
3. Surface shell guard and tool activity through runtime status/action state.
4. Add focused tests for safe shell, blocked shell, capped output, and provider activity.
5. Verify touched tests, full source tests, and build.
