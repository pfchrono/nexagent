# Plan 32-01

1. Add explicit steer state and bounded steer history in runtime session state.
   Verify: targeted runtime/provider tests show queued, deferred, and applied steer truth.

2. Surface steer state/history through command output.
   Verify: targeted CLI tests show steer status and history in `/status` / `/steer` output.

3. Remove approval ambiguity while pending action exists.
   Verify: plain `approved` resumes exact pending approval action instead of falling through as normal model prompt.
