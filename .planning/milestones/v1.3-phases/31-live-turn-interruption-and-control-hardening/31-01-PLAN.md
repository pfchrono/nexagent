# Plan 31-01

1. Fix `codex-http` request contract.
   Verify: direct live prompt through `codex-http` returns real answer.

2. Make working Codex transport become default primary path when available.
   Verify: `node dist/cli.js run "reply with one word ok"` returns bounded real answer.

3. Keep shared runtime/provider/control truth honest.
   Verify: targeted provider/runtime tests stay green.
