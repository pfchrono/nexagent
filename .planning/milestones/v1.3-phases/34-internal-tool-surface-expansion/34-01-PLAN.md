# Plan 34-01

1. Add one bounded diff/changed-work inspection tool under existing policy layer.
   Verify: targeted tool tests pass and tool list/runtime surfaces include new helper.

2. Keep prompt/tool guidance honest after expansion.
   Verify: internal tool catalog in runtime surfaces updates automatically.

3. Avoid shell-overreach.
   Verify: new helper answers common coding-inspection need without requiring generic shell fallback.
