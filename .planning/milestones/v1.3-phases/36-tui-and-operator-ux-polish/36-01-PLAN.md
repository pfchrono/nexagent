# Plan 36-01

1. Replace dashboard-first default TTY layout with workspace-first render skeleton.
   Verify: latest assistant reply and current prompt are visually primary.

2. Make diagnostics surfaces compact by default and keep deep detail behind explicit commands/expansion.
   Verify: `/status`, `/provider`, `/tools`, `/memory` no longer dump flat raw state first.

3. Improve composer ergonomics without forcing autocomplete mutations.
   Verify: visible cursor, prompt history, and preview-first suggestion flow exist or are clearly staged behind honest partial support.
