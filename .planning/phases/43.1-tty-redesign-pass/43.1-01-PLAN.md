---
phase: 43.1-tty-redesign-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.ts
  - test/cli.test.ts
autonomous: true
requirements:
  - ARC-01
must_haves:
  truths:
    - "TTY render sections show clearer hierarchy for latest reply, current task, and tool output."
    - "Workspace, reply, and composer blocks are visually distinct."
    - "No runtime semantics change: output-only presentation updates."
  artifacts:
    - "src/cli.ts"
    - "test/cli.test.ts"
  key_links:
    - "Workspace rendering helpers map to command/tool state"
    - "Task blocks consume existing state model, no direct runtime behavior swap"
---

<objective>
Deliver the first milestone iteration of TTY redesign with clear, deterministic workspace hierarchy and stronger scanability.
</objective>

<execution_context>
@/home/pfchrono/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/43.1-tty-redesign-pass/43.1-CONTEXT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<tasks>
<task type="auto">
  <name>Refactor workspace block rendering in src/cli.ts</name>
  <files>src/cli.ts</files>
  <action>
  Adjust workspace container composition so reply, current task, and tool progress blocks render with explicit role labels and consistent spacing hierarchy.
  </action>
  <verify>rg -n \"workspace|reply|tool\" src/cli.ts</verify>
  <acceptance_criteria>
    - "src/cli.ts includes distinct render path for latest reply, current task, and tool output."
    - "No changes to runtime control flow in command dispatch."
  </acceptance_criteria>
  <done>Workspace rendering hierarchy is visibly improved and functionally unchanged.</done>
</task>

<task type="auto">
  <name>Improve transcript and result emphasis</name>
  <files>src/cli.ts</files>
  <read_first>src/cli.ts</read_first>
  <action>
  Tighten spacing and visual emphasis for transcript/result lines without changing model/tool event payloads.
  </action>
  <verify>rg -n \"transcript|result|tool\" src/cli.ts</verify>
  <acceptance_criteria>
    - "Tool and transcript sections are grouped and not emitted as one flat list."
    - "Reply text remains readable in narrow terminal widths."
  </acceptance_criteria>
  <done>Render output is more scan-friendly with no command semantics changed.</done>
</task>

<task type="auto">
  <name>Guard with targeted render tests</name>
  <files>test/cli.test.ts</files>
  <action>Update tests for render shape expectations of the redesigned sections.</action>
  <verify>rg -n \"describe\\(\" test/cli.test.ts</verify>
  <acceptance_criteria>
    - "`bun test test/cli.test.ts` and `npm run build` pass."
  </acceptance_criteria>
  <done>Coverage exists for modified render paths and build still passes.</done>
</task>
</tasks>
