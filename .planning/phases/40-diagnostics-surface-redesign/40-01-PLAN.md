---
phase: diagnostics-surface-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - /status, /provider, /tools, /memory emit compact default output that is scan-friendly and one-line at most 6 lines.
    - --verbose (or verbose argument) for each command outputs grouped detailed sections with all current fields retained.
    - transcript rendering shows explicit command-result boundaries that are greppable and no longer merges command output into freeform blobs.
  artifacts:
    - src/cli.ts
  key_links:
    - command handlers in CLI for /status, /provider, /tools, /memory and the shared formatter helpers all use the same compact/verbose contract.
    - transcript renderer (`formatTranscriptEvent`/`buildChatHistoryFromSession`) includes clear command boundaries with IDs and timestamps.
---

<objective>
Harden diagnostics outputs for operator speed and safety by introducing strict compact summaries with explicit expandable detail paths for status-like commands and making transcript command-result seams explicit.

Purpose: operators must understand runtime truth quickly from `/status`, `/provider`, `/tools`, and `/memory`, then drill into details intentionally.

Output: one executable implementation plan that updates formatting + transcript boundary rendering, plus checks to prevent regressions in compact/verbose toggles.
</objective>

<execution_context>
@/home/pfchrono/code/nexagent/.planning/ROADMAP.md
@/home/pfchrono/code/nexagent/.planning/STATE.md
@/home/pfchrono/code/nexagent/.planning/phases/40-diagnostics-surface-redesign/40-AI-SPEC.md
@/home/pfchrono/code/nexagent/.planning/phases/43.1-tty-redesign-pass/43.1-01-SUMMARY.md
@/home/pfchrono/code/nexagent/src/cli.ts
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@src/cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize compact/verbose contracts for diagnostic command output</name>
  <files>src/cli.ts</files>
  <read_first>src/cli.ts</read_first>
  <action>
Refactor `formatRuntimeStatus`, `formatProviderStatus`, `formatToolPolicyStatus`, and `formatMemoryStatus` so they share a strict 2-mode contract:
- default `compact` output: one block with grouped short lines, no unbounded multiline details.
- `verbose` output: full grouped sections, same fields, multi-line detail preserved.

Add explicit section headers in compact mode for the four diagnostic commands:
- provider
- runtime
- tool-policy
- memory

Replace duplicated formatting strings with a single helper that takes `detailMode` and guarantees stable keys in both modes.

Keep current `/status`, `/provider`, `/tools`, and `/memory` command handlers unchanged at call sites; they should only pass mode + existing args into shared formatters.
  </action>
  <verify>node -e "const fs=require('fs');const t=fs.readFileSync('src/cli.ts','utf8'); console.log((t.includes('formatRuntimeStatus')&&t.includes('detailMode')&&t.includes('compact')&&t.includes('verbose'))?'formatters-ok':'formatters-missing');"</verify>
  <acceptance_criteria>
    - `src/cli.ts` contains all four formatter helpers returning both compact + verbose branches.
    - Compact output for `/status` remains under 7 lines when invoked with default args.
    - Verbose output for `/provider --verbose` includes `active`, `transport`, `capabilities`, and `caveats` lines.
  </acceptance_criteria>
  <done>All four diagnostic formatters return deterministic compact and verbose modes with explicit sectioned output.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce explicit command-result boundary rendering in transcript</name>
  <files>src/cli.ts</files>
  <read_first>src/cli.ts</read_first>
  <action>
Add a small, deterministic command boundary formatter used by `formatTranscriptEvent` and `buildChatHistoryFromSession` for command events.

Requirements:
- prepend each command event line with a stable boundary tag: `[cmd-result]` plus event summary.
- include event timestamp from `event.at` and command status (`event.status`) in transcript line.
- when detail text exists, include only the first 2 lines inline and an explicit `(+N hidden lines)` footer.
- keep `event.detail` redaction behavior unchanged.

Implementation detail:
- add helper `formatCommandBoundary(event)` returning an array of strings.
- use it from both transcript paths to avoid duplicate behavior and keep command output consistently bounded.
  </action>
  <verify>node -e "const fs=require('fs');const t=fs.readFileSync('src/cli.ts','utf8'); console.log((t.includes('formatCommandBoundary')&&t.includes('formatTranscriptEvent')&&t.includes('+')&&t.includes('hidden lines'))?'transcript-boundary-ok':'transcript-boundary-missing');"</verify>
  <acceptance_criteria>
    - `/status` command output in transcript includes a header line with `event.at` and event status.
    - command results are no longer displayed as a single unlabelled freeform blob under `agent:`.
    - transcript formatting for assistant replies remains unchanged (no command boundary text on assistant events).
  </acceptance_criteria>
  <done>Command events render with explicit boundary metadata and bounded detail preview.</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI smoke checks for compact default and verbose mode semantics</name>
  <files>src/cli.ts
test/cli.test.ts</files>
  <read_first>src/cli.ts</read_first>
  <action>
Create/extend lightweight tests that lock both modes for the diagnostics surface. If `test/cli.test.ts` is missing assertions for these paths, add targeted cases:
- `/status` default emits compact summary and excludes verbose-only keys.
- `/status --verbose` emits verbose runtime/provider/tools/memory grouped keys.
- `/tools --verbose` and `/provider --verbose` preserve field stability and include section labels.
- command transcript boundary lines contain command event tag and timestamp when a command event is rendered.

Prefer existing test helpers already used in repository; avoid snapshot-heavy assertions. Use deterministic substring checks.
  </action>
  <verify>node -e "const fs=require('fs');if(fs.existsSync('test/cli.test.ts')){console.log('tests-file-ok');} else {console.log('tests-file-missing');}"</verify>
  <acceptance_criteria>
    - `test/cli.test.ts` has at least 3 assertions tied to compact/verbose command output behavior.
    - tests check for explicit `run /status --verbose` or equivalent section header presence.
    - command transcript boundary assertion includes timestamp-like ISO prefix or event time token.
  </acceptance_criteria>
  <done>Mode semantics are covered by CI-safe tests and will fail if boundaries/verbosity regress.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test test/cli.test.ts` (or nearest test runner configured in package script)
- [ ] `npm run build`
- [ ] Manual command smoke: `printf '/status\n/provider\n/tools\n/memory\n' | bun run src/cli.ts` and verify compact outputs are one-pass readable.
- [ ] Manual command smoke with verbose flags: `/status --verbose`, `/provider --verbose`, `/tools --verbose`, `/memory --verbose` all show grouped detail.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- /status, /provider, /tools, /memory default outputs are compact and truthful by default
- verbose mode exposes full grouped detail without changing field meaning
- transcript command-result boundaries include stable command identifiers and timestamp-like markers
- No false-positive status claims introduced by formatter changes
</success_criteria>
