---
phase: 48-skill-command-baseline-skill-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.ts
  - src/runtime/session.ts
  - src/runtime/persistence.ts
  - src/models.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - "/skill lists available skills in compact table format."
    - "/skill <name> resolves deterministically: exact -> alias -> prefix -> fuzzy."
    - "$<skillname> shorthand routes using preserved raw trailing args."
    - "Unknown skill errors include deterministic top 3 closest matches plus /skill hint."
  artifacts:
    - "48-01-SUMMARY.md"
  key_links:
    - ".planning/ROADMAP.md"
    - ".planning/phases/48-skill-command-baseline-skill-skill/48-CONTEXT.md"
---

# Phase 48 Plan 01: Skill command baseline (`/skill` + `$<skillname>`)

## Objective

- **What:** Implement baseline `/skill` and `$<skillname>` command flow in terminal runtime.
- **Why:** Deliver CMD requirement surface with deterministic lookup and operator-safe fallback UX.
- **Output:** Working skill list/resolve/route baseline integrated into existing CLI command pipeline.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-skill-command-baseline-skill-skill/48-CONTEXT.md
@.planning/phases/48-skill-command-baseline-skill-skill/48-RESEARCH.md

## Tasks

<task type="code">
  <name>Skill index and deterministic resolver</name>
  <files>src/cli.ts, src/models.ts</files>
  <action>Create canonical skill index + ordered resolver pipeline (exact -> alias -> prefix -> fuzzy) used by both /skill and $&lt;skillname&gt; paths.</action>
  <verify>Resolver outputs stable single match for deterministic cases; returns ranked candidates for misses.</verify>
  <done>Lookup behavior matches locked context decisions and avoids route divergence.</done>
</task>

<task type="code">
  <name>/skill command baseline UX</name>
  <files>src/cli.ts</files>
  <action>Implement `/skill` list view (compact table) and `/skill <name>` resolve-and-route behavior, including unknown-skill guidance with top 3 closest matches and `/skill` hint.</action>
  <verify>/skill no-arg prints compact table; miss path prints deterministic top-3 suggestions.</verify>
  <done>Roadmap criteria #1, #2, #4 satisfied in runtime behavior.</done>
</task>

<task type="code">
  <name>$&lt;skillname&gt; shorthand routing with raw arg preservation</name>
  <files>src/cli.ts, src/runtime/session.ts, src/runtime/persistence.ts</files>
  <action>Add `$&lt;skillname&gt;` pre-dispatch parsing and route handoff that maps to `/skill &lt;skillname&gt; ...`, preserves raw trailing args unchanged, and stores any activation only at session scope.</action>
  <verify>$&lt;skillname&gt; routes through shared resolver and forwarded args remain byte-equivalent from input tail.</verify>
  <done>Roadmap criterion #3 satisfied; no global persistence side effects.</done>
</task>

<task type="code">
  <name>Deterministic error/report formatting and command help touch-up</name>
  <files>src/cli.ts</files>
  <action>Standardize unknown-skill error rendering and ensure help/status text mentions /skill and $&lt;skillname&gt; baseline behavior clearly.</action>
  <verify>Error format consistent across /skill and $&lt;skillname&gt; entry points with stable ordering and recovery hint.</verify>
  <done>Operator-facing guidance is clear and aligned with command behavior.</done>
</task>

## Verification

1. Run `/skill` and verify compact table output includes skill name/description/source columns.
2. Run `/skill` against exact, alias, prefix, and fuzzy resolvable names; verify deterministic route behavior.
3. Run unknown `/skill` value and confirm top 3 closest suggestions + `/skill` hint.
4. Run `$<skillname> ...args` and confirm raw trailing args are preserved end-to-end.
5. Restart session and verify activation state is session-only (no persistent global carry-over).

## Success Criteria

- [ ] `/skill` listing and lookup flow implemented per locked decisions.
- [ ] `$<skillname>` shorthand routing works with raw arg preservation.
- [ ] Unknown-skill guidance deterministic and actionable.
- [ ] Session-only persistence honored for baseline activation behavior.
