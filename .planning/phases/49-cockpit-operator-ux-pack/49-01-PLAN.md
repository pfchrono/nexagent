---
phase: 49-cockpit-operator-ux-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.ts
  - src/runtime/session.ts
  - src/tui/primitives.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Pinned flight strip always visible with mode/provider/model/context/approval/memory/risk."
    - "Per-turn action ladder shows intent->plan->act->result separate from raw trace."
    - "Warnings/blocks appear in dedicated warning card lane with suggested fix text."
    - "Pilot override row supports ABORT/HOLD/REPLAN/REQ-APPROVAL controls."
    - "Memory panel split displays session/retrieved/saved checkpoint buckets."
  artifacts:
    - "49-01-SUMMARY.md"
  key_links:
    - ".planning/ROADMAP.md"
---

# Phase 49 Plan 01: Cockpit operator UX pack

## Objective

- **What:** Implement cockpit-style operator surfaces on top of current TTY.
- **Why:** Improve command authority, situational awareness, and failure visibility.
- **Output:** New cockpit blocks and control row integrated into main workspace panel.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-cockpit-operator-ux-pack/49-CONTEXT.md

## Tasks

<task type="code">
  <name>Pinned flight strip</name>
  <files>src/cli.ts</files>
  <action>Add fixed top strip containing mode/provider/model/context left/approval/memory/risk.</action>
  <verify>Strip visible in idle and active turns; values update as state changes.</verify>
  <done>Strip never scrolls away from operator viewport.</done>
</task>

<task type="code">
  <name>Action ladder block</name>
  <files>src/cli.ts, src/runtime/session.ts</files>
  <action>Derive high-level turn phases and render dedicated intent-plan-act-result block.</action>
  <verify>Turn progression visible even when raw trace collapsed.</verify>
  <done>Operator can read current turn status without parsing trace lines.</done>
</task>

<task type="code">
  <name>Warning card lane</name>
  <files>src/cli.ts</files>
  <action>Render warning card for provider/tool/approval/clipboard failures with suggested next action.</action>
  <verify>Failure events surface warning cards above transcript body.</verify>
  <done>Critical issues no longer hidden in trace-only output.</done>
</task>

<task type="code">
  <name>Pilot override row</name>
  <files>src/cli.ts</files>
  <action>Add explicit override row for ABORT/HOLD/REPLAN/REQ-APPROVAL shortcuts and slash command mapping.</action>
  <verify>Row always visible during active turns; actions trigger expected runtime controls.</verify>
  <done>Operator override authority visible and immediate.</done>
</task>

<task type="code">
  <name>Memory panel split</name>
  <files>src/cli.ts</files>
  <action>Split memory display into session context/retrieved memory/saved checkpoints sections.</action>
  <verify>/memory and workspace panel show three memory buckets clearly.</verify>
  <done>Memory state no longer blended into one ambiguous status line.</done>
</task>

## Verification

1. Run TTY idle state and confirm pinned flight strip.
2. Execute normal prompt, approval-gated prompt, and failing prompt; inspect ladder/warning row.
3. Trigger `/memory`, `/memory session`, `/memory checkpoint`; verify panel split.
4. Validate override controls update runtime controls and trace markers.

## Success Criteria

- [ ] Flight strip pinned and accurate.
- [ ] Action ladder visible and readable.
- [ ] Warning cards elevate failures with actionable guidance.
- [ ] Pilot override row functional.
- [ ] Memory panel split visible and truthful.
