---
phase: 47-image-attachment-pipeline-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.ts
  - src/provider.ts
  - src/tui/primitives.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - "TTY composer can queue one local image for next prompt."
    - "Unsupported transport path fails with explicit operator-facing reason."
    - "Provider request payload carries image + metadata on supported transports."
  artifacts:
    - "phase summary"
  key_links:
    - ".planning/ROADMAP.md"
---

# Phase 47 Plan 01: Image attachment baseline

## Objective

- **What:** Build first image attachment path from composer to provider payload.
- **Why:** Unlock multimodal prompt baseline with explicit, safe transport gating.
- **Output:** `/attach` + `/detach`, composer attachment metadata, provider payload image parts.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Composer attachment command + preview</name>
  <files>src/cli.ts</files>
  <action>Add attachment command parsing, image validation, queued attachment state, composer/footer metadata, and send-path wiring.</action>
  <verify>Attach image with `/attach path`, see metadata in composer, send prompt.</verify>
  <done>Queued image is visible pre-send and consumed on successful turn.</done>
</task>

<task type="code">
  <name>Provider payload + transport gate</name>
  <files>src/provider.ts</files>
  <action>Add provider request attachment type, explicit unsupported path failure, and native input payload construction with image + metadata.</action>
  <verify>On `cli-exec`, attachment send returns explicit unsupported message; on http modes payload includes image + metadata text parts.</verify>
  <done>Supported transports accept attachment payload; unsupported transport fails clearly.</done>
</task>

<task type="code">
  <name>TUI repaint polish</name>
  <files>src/tui/primitives.ts, src/cli.ts</files>
  <action>Reduce screen flicker by avoiding repeated alt-screen enter/clear and skipping unchanged frame writes; compact key hint line.</action>
  <verify>Observe reduced flash during idle/render updates and shorter bottom shortcut line.</verify>
  <done>TUI no longer full-clears each frame and footer hint is condensed.</done>
</task>

## Verification

1. Run TTY and execute `/attach <image>`.
2. Confirm composer shows `image attached ...` line before send.
3. Send prompt:
   - On `cli-exec`, confirm explicit unsupported attachment error.
   - On `codex-http` or `http-responses`, confirm request succeeds with image-aware payload path.
4. Observe bottom shortcut line is compact and render flash reduced.

## Success Criteria

- [x] Operator can attach image path into composer workflow.
- [x] Unsupported provider/mode paths fail with explicit reason.
- [x] Attachment metadata surfaces in composer before send.
- [x] Request payload includes image metadata for supported transports.
