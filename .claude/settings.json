{
  "apiProvider": "codex",
  "enabledMcpjsonServers": [
    "context7",
    "github",
    "filesystem",
    "fetch",
    "sequential-thinking",
    "code-review-graph"
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "$inputJson = [Console]::In.ReadToEnd() | ConvertFrom-Json; $p = $inputJson.tool_input.file_path; if ($null -eq $p) { exit 0 }; $norm = $p -replace '\\','/'; if ($norm.EndsWith('bun.lock') -or $norm.EndsWith('package-lock.json')) { exit 2 } else { exit 0 }",
            "shell": "powershell",
            "timeout": 5,
            "statusMessage": "Checking lockfile guard"
          }
        ]
      }
    ]
  }
}
